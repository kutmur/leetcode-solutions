#include "S_1561.h"

//int Solution::maxCoins(std::vector<int>& piles) {
//    std::sort(piles.begin(), piles.end());
//    int n = piles.size();
//    int maxMoney = 0;
//
//    for (int i = n / 3; i < n; i += 2)
//        maxMoney += piles[i];
//
//    return maxMoney;
//}
