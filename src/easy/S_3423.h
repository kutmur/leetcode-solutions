#ifndef S_3423_H
#define S_3423_H

#include <vector>

class Solution {
public:
    /**
     * @brief Calculates the maximum absolute difference between adjacent elements in a vector.
     * @param nums A vector of integers.
     * @return The maximum adjacent difference. Returns 0 if the vector has fewer than 2 elements.
     */
    int maxAdjacentDistance(std::vector<int>& nums);
};

#endif // S_3423_H