#include "S_414.h"

//{
//    int largestValue;
//
//    if (numbers.size() == 1)
//        return numbers[0];
//
//    if (numbers.size() == 2)
//        return max(numbers[0], numbers[1]);
//
//
//    set<int> distinctNumbers(numbers.begin(), numbers.end());
//
//
//    if (distinctNumbers.size() < 3)
//        return *max_element(distinctNumbers.begin(), distinctNumbers.end());
//
//
//    distinctNumbers.erase(*max_element(distinctNumbers.begin(), distinctNumbers.end()));
//    distinctNumbers.erase(*max_element(distinctNumbers.begin(), distinctNumbers.end()));
//
//
//    return *max_element(distinctNumbers.begin(), distinctNumbers.end());
//    }