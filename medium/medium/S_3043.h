#pragma once
#include <vector>
#include <string>
#include <unordered_map>
#include <algorithm>

class Solution {
public:
    // Main API
    int longestCommonPrefix(std::vector<int>& arr1, std::vector<int>& arr2);
};