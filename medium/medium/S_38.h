#pragma once
#include <string>
#include <vector>

class Solution {
public:
    /**
     * Generates the n-th term of the count-and-say sequence.
     * * @param n The index of the sequence (1-based).
     * @return The string representation of the n-th term.
     */
    std::string countAndSay(int n);
};