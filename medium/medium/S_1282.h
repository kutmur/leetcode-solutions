#pragma once
#include <vector>

class S_1282 {
public:
    // Groups people based on the size of the group they belong to.
    std::vector<std::vector<int>> groupThePeople(std::vector<int>& groupSizes);
};