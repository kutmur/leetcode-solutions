#pragma once
#include <vector>
#include <algorithm>

class Solution {
public:
    // Checks if the array can be rearranged to form an arithmetic progression
    bool canMakeArithmeticProgression(std::vector<int>& arr);
};