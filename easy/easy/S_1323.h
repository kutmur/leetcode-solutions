#pragma once
#include <string>

class Solution {
public:
    /**
     * Changes at most one digit (6 -> 9) to return the maximum number.
     * @param num The input integer consisting only of digits 6 and 9.
     * @return The maximum integer obtainable.
     */
    int maximum69Number(int num);
};