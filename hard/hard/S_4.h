#pragma once
#include <vector>

class Solution {
public:
    // Finds the median of two arrays by merging and sorting (simple approach).
    double findMedianSortedArrays(std::vector<int>& nums1, std::vector<int>& nums2);
};
